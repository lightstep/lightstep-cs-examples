global:
  scrape_interval: 15s

  external_labels:
    monitor: 'jmacd-scraper'

scrape_configs:
  - job_name: 'sample-app'

    scrape_interval: 5s

    static_configs:
      - targets: ['localhost:8000']
        labels:
          _generic_namespace: 'default'
          _generic_location: 'space'
          _task_id: '123'
